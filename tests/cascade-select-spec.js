define(function(require) {
  var $ = require('$');
  var CascadeSelect = require('../src/cascade-select');
  var completeModel = CascadeSelect._completeModel;

  describe('CascadeSelect', function() {

    describe('complete model', function() {
      it('', function() {
        var model = {};
        var result = completeModel(model);
      });

    });
  });
});